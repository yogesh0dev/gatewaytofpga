<?php
/* 
 * Generated by MegaBuilder v1.0 
 * www.MegaBuilder.com
 */
 
class Price_manager extends CI_Controller{
    function __construct()
    {
        parent::__construct();
        $this->load->model('Price_manager_model');
    } 

    /*
     * Listing of price_manager
     */
    function index()
    {
        $params['limit'] = RECORDS_PER_PAGE; 
        $params['offset'] = ($this->input->get('per_page')) ? $this->input->get('per_page') : 0;
        
        $config = $this->config->item('pagination');
        $config['base_url'] = site_url('price_manager/index?');
        $config['total_rows'] = $this->Price_manager_model->get_all_price_manager_count();
        $this->pagination->initialize($config);

        $data['price_manager'] = $this->Price_manager_model->get_all_price_manager($params);
        
        $data['_view'] = 'price_manager/index';
        $this->load->view('layouts/main',$data);
    }

    /*
     * Adding a new price_manager ffffff
     */
    function add()
    {   
        $this->load->library('form_validation');

		//
		
		
		if($this->form_validation->run())     
        {   
            $params = array(
				//
				
            );
            $params=$this->security->xss_clean($params);
            $price_manager_id = $this->Price_manager_model->add_price_manager($params);
            redirect('price_manager/index');
        }
        else
        {       
			
			
				
            $data['_view'] = 'price_manager/add';
            $this->load->view('layouts/main',$data);
        }
    }  

    /*
     * Editing a price_manager
     */
    function edit($id)
    {   
        // check if the price_manager exists before trying to edit it
        $data['price_manager'] = $this->Price_manager_model->get_price_manager($id);
        
        if(isset($data['price_manager']['id']))
        {
            $this->load->library('form_validation');

			//
			
		
			if($this->form_validation->run())     
            {   
                $params = array(
					//
					
                );
				$params=$this->security->xss_clean($params);
                $this->Price_manager_model->update_price_manager($id,$params);            
                redirect('price_manager/index');
            }
            else
            {
                $data['_view'] = 'price_manager/edit';
                $this->load->view('layouts/main',$data);
            }
        }
        else
            show_error('The price_manager you are trying to edit does not exist.');
    } 

    /*
     * Deleting price_manager
     */
    function remove($id)
    {
        $price_manager = $this->Price_manager_model->get_price_manager($id);

        // check if the price_manager exists before trying to delete it
        if(isset($price_manager['id']))
        {
            $this->Price_manager_model->delete_price_manager($id);
            redirect('price_manager/index');
        }
        else
            show_error('The price_manager you are trying to delete does not exist.');
    }
    
}