<?php
/* 
 * Generated by MegaBuilder v1.0 
 * www.MegaBuilder.com
 */
 
class Adminuser extends CI_Controller{
    function __construct()
    {
        parent::__construct();
        $this->load->model('Adminuser_model');
    } 

    /*
     * Listing of adminuser
     */
    function index()
    {
        $params['limit'] = RECORDS_PER_PAGE; 
        $params['offset'] = ($this->input->get('per_page')) ? $this->input->get('per_page') : 0;
        
        $config = $this->config->item('pagination');
        $config['base_url'] = site_url('adminuser/index?');
        $config['total_rows'] = $this->Adminuser_model->get_all_adminuser_count();
        $this->pagination->initialize($config);

        $data['adminuser'] = $this->Adminuser_model->get_all_adminuser($params);
        
        $data['_view'] = 'adminuser/index';
        $this->load->view('layouts/main',$data);
    }

    /*
     * Adding a new adminuser ffffff
     */
    function add()
    {   
        $this->load->library('form_validation');

		//
		
		
		if($this->form_validation->run())     
        {   
            $params = array(
				//
				
            );
            $params=$this->security->xss_clean($params);
            $adminuser_id = $this->Adminuser_model->add_adminuser($params);
            redirect('adminuser/index');
        }
        else
        {       
			
			
				
            $data['_view'] = 'adminuser/add';
            $this->load->view('layouts/main',$data);
        }
    }  

    /*
     * Editing a adminuser
     */
    function edit($id)
    {   
        // check if the adminuser exists before trying to edit it
        $data['adminuser'] = $this->Adminuser_model->get_adminuser($id);
        
        if(isset($data['adminuser']['id']))
        {
            $this->load->library('form_validation');

			//
			
		
			if($this->form_validation->run())     
            {   
                $params = array(
					//
					
                );
				$params=$this->security->xss_clean($params);
                $this->Adminuser_model->update_adminuser($id,$params);            
                redirect('adminuser/index');
            }
            else
            {
                $data['_view'] = 'adminuser/edit';
                $this->load->view('layouts/main',$data);
            }
        }
        else
            show_error('The adminuser you are trying to edit does not exist.');
    } 

    /*
     * Deleting adminuser
     */
    function remove($id)
    {
        $adminuser = $this->Adminuser_model->get_adminuser($id);

        // check if the adminuser exists before trying to delete it
        if(isset($adminuser['id']))
        {
            $this->Adminuser_model->delete_adminuser($id);
            redirect('adminuser/index');
        }
        else
            show_error('The adminuser you are trying to delete does not exist.');
    }
    
}